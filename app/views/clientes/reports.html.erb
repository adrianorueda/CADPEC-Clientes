<h1>Lista de Clientes</h1>

   <% form_for :dump, :url=>{:controller=>"home"}, :html => { :multipart => true } do |f| -%>
 <table>
	<tr>
	  <td colspan='2'>
		<%= submit_tag 'Início'-%>
	  </td>
	</tr>
 </table>
 <% end -%>

<%= link_to 'Cadastrar cliente', new_cliente_path %> | <%= link_to 'Cadastro de clientes', clientes_path %>

<table>
  <tr>
    <th>Num. Cliente</th>
	<th>Nome</th>
    <th>Endereco</th>
    <th>Cidade</th>
    <th>Estado</th>
    <th>Cep</th>
	<th>Fone</th>
  </tr>

<% for cliente in @clientes %>
  <tr>
    <td><%=h cliente.id %></td>
	<td><%=link_to cliente.nome, cliente_path(cliente) %></td>
    <td><%=h cliente.endereco %></td>
    <td><%=h cliente.cidade %></td>
    <td><%=h cliente.estado %></td>
    <td><%=h cliente.cep %></td>
	<td>
		<%=h cliente.operadora %>
		<%=h cliente.fone %>
	</td>
    <td><%= link_to 'Exibir', cliente %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Cadastrar cliente', new_cliente_path %> | <%= link_to 'Cadastro de clientes', clientes_path %>
